name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '*/30 * * * *'  # every 30 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Streamlit app
        run: |
          echo "Pinging factor-attribution.streamlit.app…"
          curl -fsS https://factor-attribution.streamlit.app/ || true
